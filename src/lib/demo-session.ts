// Demo session timing — single source of truth so it's obvious where to
// swap in real schedule data when the API exists.

export const DEMO_SESSION = {
  nextSessionClock: "14:32",
  minutesToSession: 22,
} as const;
